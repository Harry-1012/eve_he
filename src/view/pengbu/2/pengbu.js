var data=[[355,560,4,6,0,0,26],];window.defaultData=[...data];function drawGrid(currentData){var gridContainer=document.querySelector(".grid-container");var canvas=document.getElementById("gridCanvas");canvas.innerHTML="";var oldLabels=gridContainer.querySelectorAll('.dimension-label');oldLabels.forEach(label=>label.remove());var lengthPoints=currentData[2]-1;var widthPoints=currentData[3]-1;for(let i=0;i<=lengthPoints;i++){var line=document.createElement("div");line.className="horizontal-line";line.style.top=`${(i*100)/lengthPoints}%`;canvas.appendChild(line);var leftExtension=document.createElement("div");leftExtension.className="left-extension";let pianyi=3.5;leftExtension.style.top=`calc(${(i*100)/lengthPoints}%-${pianyi}px)`;canvas.appendChild(leftExtension);if(i!==0&&i!==lengthPoints){var rightExtension=document.createElement("div");rightExtension.className="right-extension";rightExtension.style.top=`calc(${(i*100)/lengthPoints}%-${pianyi}px)`;canvas.appendChild(rightExtension);}}
for(let i=0;i<=widthPoints;i++){var line=document.createElement("div");line.className="vertical-line";line.style.left=`${(i*100)/widthPoints}%`;canvas.appendChild(line);var topExtension=document.createElement("div");topExtension.className="top-extension";let pianyi=3.8;topExtension.style.left=`calc(${(i*100)/widthPoints}%-${pianyi}px)`;canvas.appendChild(topExtension);var bottomExtension=document.createElement("div");bottomExtension.className="bottom-extension";bottomExtension.style.left=`calc(${(i*100)/widthPoints}%-${pianyi}px)`;canvas.appendChild(bottomExtension);if(i!==widthPoints){var topExtension=document.createElement("div");topExtension.className="top-extension";let pianyi=3.8;topExtension.style.left=`calc(${(i*100)/widthPoints}%-${pianyi}px)`;canvas.appendChild(topExtension);}
if(i!==widthPoints){var bottomExtension=document.createElement("div");bottomExtension.className="bottom-extension";bottomExtension.style.left=`calc(${(i*100)/widthPoints}%-${pianyi}px)`;canvas.appendChild(bottomExtension);}}
for(let i=0;i<=widthPoints;i++){for(let j=0;j<=lengthPoints;j++){if(i===widthPoints&&(j===0||j===lengthPoints)){continue;}
var point=document.createElement("div");point.className="anchor-point";point.style.left=`${(i*100)/widthPoints}%`;point.style.top=`${(j*100)/lengthPoints}%`;canvas.appendChild(point);}}}
drawGrid(data[0]);